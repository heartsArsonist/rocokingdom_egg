# 🥚 洛克王国 · 异色生蛋智能配窝助手 — 项目结构

> 版本：V6.1
> 作者：薯多芬上校  
> 技术栈：原生 HTML + CSS + JavaScript（ES5），无框架依赖

---

## 📁 目录一览

```
7.1/
├── index.html              ← 页面入口（含 9 个弹窗骨架）
├── style.css               ← 统一样式表（CSS 变量体系 · 35 分区）
├── 项目结构.md              ← 本文件
├── README.md               ← 用户文档
├── data/
│   ├── pets.json           ← 全精灵数据库（ID、名称、蛋组、进化链、标签）
│   ├── defines.json        ← 蛋组/赛季/特殊标签的中文名称定义
│   ├── personalities.json  ← 性格数据 { boost: [{ name, decrease }] }
│   └── medals.json         ← 奖牌数据 { seriesKey: [{ id, name, icon }] }（分组结构）
└── js/
    ├── state.js            ← 全局状态（仓库 + 窝 + 筛选）
    ├── dataLoader.js       ← 数据加载、兼容性构建、进化链
    ├── uiHelpers.js        ← UI 辅助 · 弹窗 · 标签 · 仓库管理 · 窝管理
    ├── recommend.js        ← 配窝推荐算法
    ├── replace.js          ← 替换雄性弹窗（兼容性覆盖匹配）
    ├── placement.js        ← 位置图生成
    ├── svgRender.js        ← SVG 渲染 + 拖动
    ├── importExport.js     ← 配置导入/导出（新旧格式兼容）
    └── main.js             ← 事件绑定 · combo 工厂 · 初始化
```

---

## 📄 文件说明

### `index.html`
- 三大区域：**精灵库存面板** / **精灵窝面板** / 结果区
- 九大弹窗：主录入弹窗、管理仓库弹窗、仓库编辑弹窗、入窝弹窗、入窝属性弹窗、替换弹窗、性格弹窗、奖牌弹窗
- 雄性筛选栏（异色 / 性格 / body 奖牌 / voice 奖牌）
- 引入 `html2canvas`（导出图片用）

### `style.css`
- 设计令牌 (`:root`)：**色板 / 字号(7级) / 间距(3级) / 过渡(2级) / 圆角(6级) / 性格&奖牌配色**
- 35 个分区，按页面顺序编排
- 命名体系：
  - `.panel-tag-pers` / `.panel-tag-medal` — 面板标签（浅底深字）
  - `.nest-tag-pers` / `.nest-tag-medal` — 方案标签（深底白字）
  - `.nest-star-badge` / `.nest-uncovered-overlay` — 方案叠加层
  - `.sub-modal-*` — 副弹窗统一骨架
  - `.stock-*` — 库存面板独立样式
  - `.inv-modal-*` — 仓库管理弹窗
  - `.inv-edit-*` — 编辑弹窗
  - `.nest-select-card` — 入窝选择卡片

### `data/pets.json`
- 字段：`id`, `name`, `egg_groups`, `evolves_from_id`, `special_tags`, `has_shiny`

### `data/defines.json`
- `egg_groups` / `season` / `special_tags` 中文映射

### `data/personalities.json`
- 结构：`{ "物攻": [{ name: "胆小", decrease: "速度" }], ... }`

### `data/medals.json`
- **分组结构**：`{ "body": [{ id, name, icon }], "voice": [{ id, name, icon }] }`
- 每个系列独立选取，互不冲突

---

### `js/state.js` — 全局状态

- **统一仓库**：`inventory[]` — 所有已录入精灵（含性别/异色/性格/奖牌）
- **入窝雌性**：`nestFemales[]` — 已放入窝的雌性（从仓库选取）
- **实例级存储**：每只精灵独立存储 `{ species, gender, shiny, personality, medals: { body: "dkt", voice: null } }`
- 模态框临时状态：`modalTempCounts` / `modalTempShinyCounts` / `modalTempPersonalities` / `modalTempMedals`
- 推荐结果缓存：`lastResultData`
- 雄性筛选条件：`filterOnlyShiny` / `filterPersonality` / `filterMedals: { body, voice }`
- 网格常量：`GRID_SIZE = 7` / `FINE_GRID = 14`

### `js/dataLoader.js` — 数据层

- `loadPetsJSON()` — 加载精灵 → 构建兼容表
- `loadDefinitions()` — 加载中文名 + 性格 + 奖牌
- `buildCompatibilityMap()` — 构建可交配关系图
- `getEvolutionChain()` — BFS 进化链
- `getPersonalityByName(name)` / `getMedalById(id)` — 查询工具
- `getMedalSeriesKeys()` — 获取所有奖牌系列键名
- `fillFilterSelects()` — 填充雄性筛选栏自定义 combobox（性格 + 每个系列奖牌独立 combo）
- `selectPersonality()` / `selectMedalForSeries()` — 筛选选中 + 互斥关闭

### `js/uiHelpers.js` — UI 辅助（最大模块，~850 行）

**库存面板**
- `renderInventoryCards()` — 渲染只读库存卡片（含蛋组/性格/奖牌/异色标记）
- `renderInvCardContent()` — 卡片 HTML 生成
- `invFilterGender` / `invFilterShiny` / `invFilterPers` / `invFilterMedalBody` / `invFilterMedalVoice` — 展窗筛选状态

**管理仓库弹窗**
- `openInventoryModal()` / `closeInventoryModal()` — 仓库弹窗
- `renderInvModalList()` — 仓库列表（筛选 + 搜索 + 分组）
- `invModalFilterClick(val, btn)` — 性别筛选（内联 onclick）

**仓库编辑弹窗**
- `openInvEditPopup()` — 编辑属性（数量/性格/body/voice）
- `saveInvEdit()` / `deleteInvEditEntry()` / `closeInvEdit()` — 保存/删除/关闭

**入窝弹窗**
- `openNestFemaleModal()` / `toggleNestMode()` — 入窝弹窗（两种模式）
- `performNestSearch()` — 搜索 + 蛋组/赛季筛选
- `addNestFemale()` / `renderNestModalSelected()` / `confirmNestFemales()` — 已选列表管理
- `openNestAttrPopup()` / `confirmNestAttr()` — 全部精灵模式下选择属性

**主录入弹窗**（沿用旧逻辑）
- `openModal()` / `closeModal()` / `performFilteredSearch()`
- 数量按钮使用内联 `onclick`（`modalQtyChange` / `modalQtySet` / `modalShinyChange` / `modalShinySet`）

**性格/奖牌弹窗** — `openPersonalityModal()` / `openMedalModal()`

### `js/recommend.js` — 配窝推荐

- `computeRecommendation()`：
  1. `nestFemales` → 雌性实例
  2. `inventory(male)` → 按筛选条件过滤
  3. 唯一依赖锁定 → 贪心填充 → 局部搜索（Hill Climbing + Swap）
  4. 兜底踢除
- `maxMatching()` — 匈牙利算法
- `renderResult()` — 渲染方案到 DOM
- `renderNestMedalTags()` — medals 对象 → HTML 标签

### `js/replace.js` — 替换雄性

- `openReplaceModal(slotIndex)` — 候选判定改为**实际覆盖能力匹配**（`compatibleMap` 直接判断候选是否能覆盖全部被覆盖雌性）
- 筛选：异色 / 性格 / body / voice / 关键词
- `createMiniCombo()` — 迷你下拉框工厂（替换弹窗专用）
- `doReplaceMale()` — 执行替换 + 重算标号

### `js/placement.js` — 位置图（~800 行）

- `calcEffectiveEggGroups()` — 有效蛋组
- `generatePlacement()` — 聚类检测 → 确定性放置 → 求解器 → 居中对齐
- `solvePlacement()` / `solvePlacementFine()` — 整数格 / 细网格求解器
- `findNearestFreePositionHalf()` — 半格精度拖拽

### `js/svgRender.js` — SVG 可视化

- `SVG_COLORS` — 14 个颜色常量
- `renderSVG()` — 7×7 渲染 + 14×14 细网格
- `createAttrLabel()` — 离屏 SVG 精确测量文本宽度
- 拖动交互：Chebyshev 封锁防重叠

### `js/importExport.js` — 配置导入/导出

- 新格式：`inventory[]` + `nestFemales[]`（含 `medals: { body, voice }`）
- 旧版兼容：`females[]` + `males[]`（含 `medal` 单字段 → 自动转到第一系列）

### `js/main.js` — 事件绑定 + combo 工厂

- `buildCombo()` — 统一的 combo 创建工厂（展窗 ×3 / 仓库 ×3 / 窝弹窗 ×3）
- `addComboGroupReset()` — 每组 combo 追加 ↺ 重置按钮
- `fillAllCombos()` — 数据加载后统一填充所有下拉
- 事件绑定：主弹窗、仓库弹窗、编辑弹窗、展窗筛选、窝弹窗、替换弹窗、雄性筛选、按钮

---

## 🔗 文件加载顺序

```
state.js → dataLoader.js → uiHelpers.js → recommend.js 
  → replace.js → placement.js → svgRender.js 
  → importExport.js → main.js
```

全局 `var` 按此顺序累积，不可调换。

---

## 🧠 核心算法流程

```
用户管理仓库（录入雌/雄，设置性格/奖牌）
  ↓
用户放置雌性入窝（从仓库选或从全精灵搜索）
  ↓
雄性筛选过滤（异色⭐ / 性格🎭 / body🏅 / voice🏅）
  ↓
computeRecommendation()
  ├─ 雌性入窝实例 + 雄性库存候选
  ├─ 唯一依赖锁定
  ├─ 贪心初始解（prefer newC, then total）
  ├─ 局部搜索 Swap（maxMatching × 10000 + covered）
  └─ 兜底踢除
  ↓
renderResult() → 查看方案 → 可选替换雄性（按实际覆盖兼容判定）
  ↓
generatePlacement()
  ├─ 聚类检测 → 确定性放置 → 求解器 → 居中对齐
  └─ renderSVG()
  ↓
拖动调整 → 导出位置图
```

---

## 🎨 UI 设计体系

| 令牌类别 | 变量 | 数量 |
|---|---|---|
| 色板 | `--bg`, `--card`, `--accent`, `--female`, `--male`, `--warn`, `--ok`, `--text`, `--text-soft`, `--text-strong`, `--link-accent`, `--card-light`, `--border-soft` | 13 |
| 字号 | `--fs-xs` ~ `--fs-3xl` | 7 级 |
| 间距 | `--sp-xs` / `--sp-sm` / `--sp-md` | 3 级 |
| 过渡 | `--tr-fast` / `--tr-slow` | 2 级 |
| 圆角 | `--r-container` ~ `--r-micro` | 6 级 |
| 性格/奖牌 | `--pers-*` / `--medal-*` | 8 个（深浅各一套） |

---

## 📌 命名约定

| 位置 | 性格标签 | 奖牌标签 |
|---|---|---|
| 面板 | `.panel-tag-pers` | `.panel-tag-medal` |
| 方案格 | `.nest-tag-pers`（深底白字） | `.nest-tag-medal`（深底白字） |
| 库存卡片 | `.stock-card-top` | `.stock-card-bottom` |
| SVG | `SVG_COLORS.persTagBg` | `SVG_COLORS.medalTagBg` |

统一弹窗骨架：`.sub-modal-overlay` > `.sub-modal-box` > `.sub-modal-header` / `.sub-modal-info` / `.sub-modal-body` / `.sub-modal-footer`
```

